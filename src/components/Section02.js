export default function Section02({ darkMode }) {
  return (
    <section id="section02" className={(darkMode ? "bg-capx-dark-box-bg " : "bg-capx-light-box-bg ") + "flex w-full h-screen"}></section>
  )
}